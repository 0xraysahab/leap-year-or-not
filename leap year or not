include <stdio.h>

int main() {
    int yr;
    printf("enter year:");
    scanf("%d",&yr);
    if(yr%4==0 && yr%100!=0){
        printf("year is leap year");
    }
    else{
        printf("not leap year");
    }

    return 0;
}
